import {
  matrixProp,
  CircuitValue,
  Field,
  SmartContract,
  method,
  Bool,
  state,
  State,
  isReady,
  Poseidon,
  Permissions,
  Mina,
  Party,
  PrivateKey,
  PublicKey,
} from 'snarkyjs';

@method init() {
  // Define initial values of on-chain state
  // registered thru onboarding process
  
  this.buyer1.set(users['buyer1'].toPublicKey());
  this.buyer2.set(users['buyer2'].toPublicKey());
  this.buyer3.set(users['buyer3'].toPublicKey());
  this.buyer4.set(users['buyer4'].toPublicKey());
  this.buyer5.set(users['buyer5'].toPublicKey());
  this.buyer6.set(users['buyer6'].toPublicKey());
  this.buyer7.set(users['buyer7'].toPublicKey());
  this.buyer8.set(users['buyer8'].toPublicKey());
  this.buyer9.set(users['buyer9'].toPublicKey());
  this.seller1.set(users['seller1'].toPublicKey());
  this.seller2.set(users['seller2'].toPublicKey());
  this.seller3.set(users['seller3'].toPublicKey());
  this.seller4.set(users['seller4'].toPublicKey());
  this.seller5.set(users['seller5'].toPublicKey());
  this.seller6.set(users['seller6'].toPublicKey());
  this.seller7.set(users['seller7'].toPublicKey());
  this.seller8.set(users['seller8'].toPublicKey());
  this.seller9.set(users['seller9'].toPublicKey());
  
  
  
  
  
  
  
  this.message.set(Field(0));
  this.messageHistoryHash.set(Field(0));
}

@method publishBid (bid: Field, signerPrivateKey: PrivateKey)  {


}

@method publishAsk (ask: Field, signerPrivateKey: PrivateKey)  {

}

// measure time in block height
